// Numbers, Strings, and Booleans

let myScore = 1055; //mutable
const PLAYER_NAME = "Luigi"; //immutable
var myScore2 = 1056; //old, is only mutable and can cause issues in ES6 and above.  Can be seen in legacy code.

// Null - empty and non-existant values.
// Undefined - variables that have been declared, but not initialized.
// Functions can also be undefined.